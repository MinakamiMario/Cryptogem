# Quick Load-in — Cryptogem

Context: `docs/CONTEXT_CAPSULE.md` v1.0.
Canonical key: `time_max_bars` (niet `tm_bars`). Migratie: `normalize_cfg()`.
Grid filtering: `PARAMS_BY_EXIT` in `agent_team_v3.py`.
Tests: 66 in `test_param_sensitivity.py` — moeten groen blijven.
Output = artifacts. Context ontbreekt? → `CONTEXT_MISSING`, geen aannames.
Open: score rounding fix + head_to_head.py migrate + 5×2h re-run.
